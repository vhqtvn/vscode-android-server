#!/bin/sh
echo $NDK_BIN_ROOT/$ANDROID_ARCH-linux-android$ANDROID_BUILD_API_VERSION-clang -fPIC $(arg-hook $@) $@
patch-hook -fPIC $@
$NDK_BIN_ROOT/$ANDROID_ARCH-linux-android$ANDROID_BUILD_API_VERSION-clang -fPIC $(arg-hook $@) $@
